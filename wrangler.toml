name = "tenzoxauth-cfire"
main = "src/index.js"
compatibility_date = "2024-03-20"

[assets]
directory = "./dist"
binding = "ASSETS"

# Variables and secrets should be provided via .dev.vars for local development
# or via `wrangler secret put` for production deployments.
